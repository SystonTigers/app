{
  "name": "@team-platform/sdk",
  "version": "1.0.0",
  "description": "Shared TypeScript SDK for multi-tenant team platform API",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "clean": "rm -rf dist",
    "test": "npm run build && node --test"
  },
  "keywords": [
    "team-platform",
    "multi-tenant",
    "api-client",
    "white-label"
  ],
  "author": "Team Platform",
  "license": "MIT",
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.3.0"
  }
}
