// backend/src/services/reminders.ts
export async function sendEventReminders(env: any) {
  // TODO: query upcoming events (e.g., in next 60–120 mins) and
  // use push service to notify attendees.
  // Keep empty for now; we'll fill this once events data model is finalized.
  return { ok: true };
}
